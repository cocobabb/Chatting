spring.application.name=chat

# CORS
DEVELOP_FRONT_ADDRESS=${DEVELOP_FRONT_ADDRESS}

# ========== Database ==========
spring.datasource.url=jdbc:mysql://${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== JPA  ==========
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

#========= MogoDB ===============
spring.data.mongodb.uri=mongodb+srv://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@${MONGODB_CLUSTER}uster.houvjvg.mongodb.net/${MONGODB_DATABASE}?retryWrites=true&w=majority&appName=myCluster
